@inject NavigationManager NavigationManager

<div class="main-menu-container">
    <div class="menu-header">
        <span class="menu-title">Main Menu</span>
        <button class="expand-all-btn" @onclick="ToggleExpandAll">
            <span>@(allExpanded ? "Collapse All" : "Expand All")</span>
            <i class="bi @(allExpanded ? "bi-chevron-up" : "bi-chevron-down") ms-1"></i>
        </button>
    </div>

    <div class="menu-scrollable">
        <!-- Main Page Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleMainPage">
                <i class="bi bi-chevron-right chevron-icon @(isMainPageExpanded ? "expanded" : "")"></i>
                <i class="bi bi-house-door-fill group-icon"></i>
                <span class="group-title">Main Page</span>
            </div>
            @if (isMainPageExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="document-browser" Match="NavLinkMatch.All">
                        <i class="bi bi-folder-fill item-icon"></i>
                        <span>Document Browser</span>
                    </NavLink>
                    <NavLink class="menu-item" href="registration-document">
                        <i class="bi bi-file-earmark-text-fill item-icon"></i>
                        <span>Registration Document</span>
                    </NavLink>
                    <NavLink class="menu-item" href="density-chart">
                        <i class="bi bi-bar-chart-steps item-icon"></i>
                        <span>Density Chart</span>
                    </NavLink>
                    <NavLink class="menu-item" href="room-status">
                        <i class="bi bi-grid-3x3-gap-fill item-icon"></i>
                        <span>Room Status</span>
                    </NavLink>
                    <NavLink class="menu-item" href="room-inventory">
                        <i class="bi bi-key-fill item-icon"></i>
                        <span>Room Inventory</span>
                    </NavLink>
                    <NavLink class="menu-item" href="package-audit">
                        <i class="bi bi-file-earmark-check-fill item-icon"></i>
                        <span>Package Audit</span>
                    </NavLink>
                </div>
            }
        </div>

        <!-- Registration Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleRegistration">
                <i class="bi bi-chevron-right chevron-icon @(isRegistrationExpanded ? "expanded" : "")"></i>
                <i class="bi bi-journal-text group-icon"></i>
                <span class="group-title">Registration</span>
            </div>
            @if (isRegistrationExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="reservation">
                        <i class="bi bi-calendar-event-fill item-icon"></i>
                        <span>Reservation</span>
                    </NavLink>
                    <NavLink class="menu-item" href="checkin">
                        <i class="bi bi-plus-square-fill item-icon"></i>
                        <span>Counter (Check In)</span>
                    </NavLink>
                    <NavLink class="menu-item" href="group-registration">
                        <i class="bi bi-people-fill item-icon"></i>
                        <span>Group Registration</span>
                    </NavLink>
                </div>
            }
        </div>

        <!-- Cashiering Group -->
        <!-- <div class="menu-group">
            <div class="group-header" @onclick="ToggleCashiering">
                <i class="bi bi-chevron-right chevron-icon @(isCashieringExpanded ? "expanded" : "")"></i>
                <i class="bi bi-cash-stack group-icon"></i>
                <span class="group-title">Cashiering</span>
            </div>
            @if (isCashieringExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="registration-document">
                        <i class="bi bi-receipt item-icon"></i>
                        <span>Manual Charge</span>
                    </NavLink>
                </div>
            }
        </div> -->

        <!-- Profile Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleProfile">
                <i class="bi bi-chevron-right chevron-icon @(isProfileExpanded ? "expanded" : "")"></i>
                <i class="bi bi-person-fill group-icon"></i>
                <span class="group-title">Profile</span>
            </div>
            @if (isProfileExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="guest">
                        <i class="bi bi-person item-icon"></i>
                        <span>Guest</span>
                    </NavLink>
                    <NavLink class="menu-item" href="contact">
                        <i class="bi bi-telephone-fill item-icon"></i>
                        <span>Contact</span>
                    </NavLink>
                    <NavLink class="menu-item" href="company">
                        <i class="bi bi-building item-icon"></i>
                        <span>Company</span>
                    </NavLink>
                    <NavLink class="menu-item" href="travel-agent">
                        <i class="bi bi-airplane-fill item-icon"></i>
                        <span>Travel Agent</span>
                    </NavLink>
                    <NavLink class="menu-item" href="source">
                        <i class="bi bi-share-fill item-icon"></i>
                        <span>Source</span>
                    </NavLink>
                    <NavLink class="menu-item" href="group">
                        <i class="bi bi-people item-icon"></i>
                        <span>Group</span>
                    </NavLink>
                </div>
            }
        </div>

        <!-- Night Audit Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleNightAudit">
                <i class="bi bi-chevron-right chevron-icon @(isNightAuditExpanded ? "expanded" : "")"></i>
                <i class="bi bi-moon-stars-fill group-icon"></i>
                <span class="group-title">Night Audit</span>
            </div>
            @if (isNightAuditExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="end-of-day">
                        <i class="bi bi-brightness-alt-high-fill item-icon"></i>
                        <span>End of Day</span>
                    </NavLink>
                    <NavLink class="menu-item" href="end-of-month">
                        <i class="bi bi-calendar-check-fill item-icon"></i>
                        <span>End of Month</span>
                    </NavLink>
                </div>
            }
        </div>

        <!-- Reports Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleReports">
                <i class="bi bi-chevron-right chevron-icon @(isReportsExpanded ? "expanded" : "")"></i>
                <i class="bi bi-file-earmark-bar-graph-fill group-icon"></i>
                <span class="group-title">Reports</span>
            </div>
            @if (isReportsExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="reports">
                        <i class="bi bi-bar-chart-line-fill item-icon"></i>
                        <span>Reports</span>
                    </NavLink>
                </div>
            }
        </div>

        <!-- Settings Group -->
        <div class="menu-group">
            <div class="group-header" @onclick="ToggleSettings">
                <i class="bi bi-chevron-right chevron-icon @(isSettingsExpanded ? "expanded" : "")"></i>
                <i class="bi bi-gear group-icon"></i>
                <span class="group-title">Setting And Miscellaneous</span>
            </div>
            @if (isSettingsExpanded)
            {
                <div class="group-items">
                    <NavLink class="menu-item" href="property">
                        <i class="bi bi-house-fill item-icon"></i>
                        <span>Property</span>
                    </NavLink>
                    <NavLink class="menu-item" href="package">
                        <i class="bi bi-box-seam-fill item-icon"></i>
                        <span>Package</span>
                    </NavLink>
                    <NavLink class="menu-item" href="revenue">
                        <i class="bi bi-cash-coin item-icon"></i>
                        <span>Revenue Management</span>
                    </NavLink>
                    <NavLink class="menu-item" href="calendar">
                        <i class="bi bi-calendar3 item-icon"></i>
                        <span>Calendar</span>
                    </NavLink>
                    <NavLink class="menu-item" href="budget">
                        <i class="bi bi-wallet2 item-icon"></i>
                        <span>Budget</span>
                    </NavLink>
                    <NavLink class="menu-item" href="license">
                        <i class="bi bi-key-fill item-icon"></i>
                        <span>License</span>
                    </NavLink>
                    <NavLink class="menu-item" href="update">
                        <i class="bi bi-arrow-clockwise item-icon"></i>
                        <span>ERP Update</span>
                    </NavLink>
                </div>
            }
        </div>
    </div>

    <div class="menu-footer">
        <a class="logout-link" href="javascript:void(0)" @onclick="Logout">
            <i class="bi bi-box-arrow-left logout-icon"></i>
            <span>Log Out</span>
        </a>
    </div>
</div>

@code {
    private bool isMainPageExpanded = true;
    private bool isRegistrationExpanded = true;
    private bool isCashieringExpanded = true;
    private bool isProfileExpanded = true;
    private bool isNightAuditExpanded = true;
    private bool isReportsExpanded = true;
    private bool isSettingsExpanded = true;

    private bool allExpanded = true;

    private void ToggleMainPage() => isMainPageExpanded = !isMainPageExpanded;
    private void ToggleRegistration() => isRegistrationExpanded = !isRegistrationExpanded;
    private void ToggleCashiering() => isCashieringExpanded = !isCashieringExpanded;
    private void ToggleProfile() => isProfileExpanded = !isProfileExpanded;
    private void ToggleNightAudit() => isNightAuditExpanded = !isNightAuditExpanded;
    private void ToggleReports() => isReportsExpanded = !isReportsExpanded;
    private void ToggleSettings() => isSettingsExpanded = !isSettingsExpanded;

    private void Logout() => NavigationManager.NavigateTo("/account/logout", forceLoad: true);

    private void ToggleExpandAll()
    {
        allExpanded = !allExpanded;
        isMainPageExpanded = allExpanded;
        isRegistrationExpanded = allExpanded;
        isCashieringExpanded = allExpanded;
        isProfileExpanded = allExpanded;
        isNightAuditExpanded = allExpanded;
        isReportsExpanded = allExpanded;
        isSettingsExpanded = allExpanded;
    }
}
